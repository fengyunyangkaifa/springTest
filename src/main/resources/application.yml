server:
  port: 9090
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/home2?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    password: root
    driver-class-name: com.mysql.jdbc.Driver
    username: root
mapper:
  mappers: tk.mybatis.mapper.common.Mapper
  not-empty: false
  identity: MYSQL
  style: camelhump
#mybatis分页配置
pagehelper:
  helper-dialect: mysql
  reasonable: true
  support-methods-arguments: true
  params: count=countsql